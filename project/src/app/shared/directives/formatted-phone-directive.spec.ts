import { FormattedPhoneDirective } from './formatted-phone-directive';

describe('FormattedPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new FormattedPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
